import {
  Virtualizer,
  observeWindowOffset,
  observeWindowRect,
  windowScroll,
} from '@tanstack/virtual-core';

window.ElecKoiTanStackVirtual = Object.freeze({
  Virtualizer,
  observeWindowOffset,
  observeWindowRect,
  windowScroll,
});
